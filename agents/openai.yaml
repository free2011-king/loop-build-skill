interface:
  display_name: "Loop Builder"
  short_description: "Scaffold loops with roles, covenants, and feedback"
  default_prompt: "Use $loop-builder to turn this goal into a reusable Loop with role selection, implementation covenant, evidence records, feedback signals, and update gates."
